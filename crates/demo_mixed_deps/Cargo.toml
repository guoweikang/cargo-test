[package]
name = "demo_mixed_deps"
version = "0.1.0"
edition = "2021"

[package.metadata.kbuild]
enabled = true

[dependencies]
kbuild_config = { path = "../kbuild_config" }

# No features needed - this crate uses config constants from kbuild_config
# It doesn't have optional dependencies
